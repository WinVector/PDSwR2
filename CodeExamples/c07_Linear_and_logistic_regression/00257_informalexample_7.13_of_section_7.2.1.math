# informalexample 7.13 of section 7.2.1 
# (informalexample 7.13 of section 7.2.1)  : Linear and logistic regression : Using logistic regression : Understanding logistic regression 

log_odds[flight_delayed] = log(P[flight_delayed == TRUE] / P[flight_delayed == FALSE])

Let: p = P[flight_delayed == TRUE]; then
log_odds[flight_delayed] = log( p / (1 - p) )

